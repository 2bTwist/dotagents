# perf (optional)

`perf-harness-init` and `perf-loop`, moved out of the default skill set on
2026-09-11. See `specs/decisions/2026-09-11-trim-baseline-before-building.md`.

Install: `./install.sh --with-optional`. Remove: delete the two directories from
`~/.claude/skills` (or the harness equivalent).
